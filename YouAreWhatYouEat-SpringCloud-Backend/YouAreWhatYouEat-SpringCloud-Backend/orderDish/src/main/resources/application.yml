server:
  port: 18090 # port

spring:
  application:
    name: orderDish # ???????
  jpa:
    database: oracle

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:18082/eureka/
  instance:
    prefer-ip-address: true

