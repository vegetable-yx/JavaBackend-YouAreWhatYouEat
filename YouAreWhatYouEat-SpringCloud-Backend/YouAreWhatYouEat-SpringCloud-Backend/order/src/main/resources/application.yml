server:
  port: 18089 # port

spring:
  application:
    name: order # ???????
  jpa:
    database: oracle

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:18082/eureka/
  instance:
    prefer-ip-address: true

